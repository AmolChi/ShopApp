import { CartItem } from "@/types"
import { Text, View } from "react-native"


const CartCard = (pizza:CartItem|any) => {
  return (
    <View>
        <Text>

        </Text>
    </View>
  )
}

export default CartCard